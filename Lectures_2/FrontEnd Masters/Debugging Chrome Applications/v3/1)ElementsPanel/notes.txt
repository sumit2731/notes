you can edit these things -
    tags(chnage starting tag, ending will be chnaged automatically)
    attributes,css selectrors(in style panel),css proeprties(in style panel), values(in style panel)

Also we can click on new style rule and a new css selector will be added in styles panel. dev tools automatically makes this selector,
    in such a way that it has highest priority.but you can edit this selector.

you can click on .cls and add new classes to selected element.

also you can drag and drop DOM nodes, even nest them inside each other.

everything here is double clickable, so you can double click and edit it.