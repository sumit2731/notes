1)to convert something into number-
    let a = parseFloat(b);
    or
    let b = +a;
 this is useful to convert string into number, if it cannot be converted into number,
 then we will get Nan.

2)to convert something into string-
    let a = b.toString();

3) a = a+1 is equivlaent to a += 1

4)Declaring multiple variables in single line-
    let a = 1, b= 2, c= 3.
    let a = b = c= 1;
    let [a,b,c] = [1,2,3];  Array Destructuring
    let {a,b,c} = {a: 1, b: 2, c:3} Object Destructuring

5)How to swap 2 elements(i and j) of array =
    [arr[i], arr[j]] = [arr[j], arr[i]]

6)How tp convert a iterable into a array-
    1)let array1 = Array.from(iterable);
    2)let array1 = [...iterable1];

7)How to create large arrays with some lements inside them-

    1)Empty Array
        a)let arr1 = new Array(50); // this will create array of 50 elements. each element is undefined

        b)let arr2 = Array.from({length: 50}); // each value will be undefined
    
    2)Array with some values
        a)let arr1 = new Array(50).join('1.1').split();
        
        b)let arr2 = Array.from({length: 50}, () => [])

    go throught this article-
        https://medium.com/javascript-everyday/javascript-array-from-53287c195487

8)Trick to remove some elements from string-
    let newString = string
        .split("")
        .filter((char) => [chars[i], chars[j]].indexOf(char) != -1)
        .join("");

9)how to remove  dulicates from array
    let oldArray = [1,2,3,3,1,2,4,5];
    let newArray = Array.from(new Set(oldArray));