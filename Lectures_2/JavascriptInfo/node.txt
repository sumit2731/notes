1)nodeName = a)for elements it is same a tagName, like - BODY
             b)for other node types (text, comment, etc.) it has a string
              with the node type

2)nodeType = elem.nodeType == 1 for element nodes
             elem.nodeType == 3 for text nodes,
             elem.nodeType == 9 for the document object

3)nodeName = For elements it means the same as tagName.
             for other node types (text, comment, etc.) it has a string with the node type
             like it returns- #text,#body.

4)nodeValue/data = The innerHTML property is only valid for element nodes.Other node types,
 such as text nodes, have their counterpart: nodeValue and data. we will use data, as 
 it is shorter.lets see 
     <body>
        Hello
        <!-- Comment -->
        <script>
            let text = document.body.firstChild;
            alert(text.data); // Hello

            let comment = text.nextSibling;
            alert(comment.data); // Comment
        </script>
    </body>

