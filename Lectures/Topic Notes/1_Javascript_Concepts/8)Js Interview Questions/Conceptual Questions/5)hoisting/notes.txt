1)function declarations are added to the memory during the compile stage, so we are able to access it in our code before the 
    actual function declaration.
2)let and const variable are hoisted but not initialized with udenfined.