/**
 * we also noted the difference between HTML and JSX, where JSX focuses on property names rather than HTML attributes, and that the 
 * style prop also is specifically the object just like it is for the DOM as well.
 * 
 * Step 1 - Here we applied style on our react components tthrough classes and styles props
 * Step 2 - Here we created a component and applied common styles to it, while providing support for provides extra classes and style
 *  from outisde.here we used the spread props
 * Step 3 - Here we encapsulated the style logic, by just passing the style prop.
 */