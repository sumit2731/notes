import { myFunc } from "../060-modules-scripts-and-declaration-files/164-declaration-files-can-be-used-to-type-js-files.solution/src/example";

/**
 * Adds two numbers together.
 *
 * @example
 *
 * myFunction(1, 2);
 */
const myFunction = (a: number, b: number) => {
  return a + b;
};
