/**
 * @call and apply - call and apply methods we can invoke the function with the new context.
 * The values will be attached to that execution only.
 */

/**
 * @bind - When using bind, we can create a new function with the new values and store it in a
 * variable, and then use it further. It creates fresh permanent binding without affecting the
 * original function.
 */
