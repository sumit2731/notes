/**
 * Note - About micro-optimization on UI. befire we make double array iteration into consideration
 * there are other problems that we will likely run into -
 *  a)We will run out of memory into user's device
 *  b)long wait time before we get items back for user
 */
