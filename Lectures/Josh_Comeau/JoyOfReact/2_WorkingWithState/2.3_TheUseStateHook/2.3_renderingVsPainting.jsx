/**
 *Re-rendering - When component function is executed to see, what has changed.so we get new jsx now reconciliation algorithm begins

 Painting - when actually DOM is changed then by javascript, browser needs to do repainting.
 Each re-render does not lead to re-painting.reacts updates the minimal amount of DOM
 */
