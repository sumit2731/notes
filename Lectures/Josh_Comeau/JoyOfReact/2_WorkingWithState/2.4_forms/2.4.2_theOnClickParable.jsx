/**
 * This explain how we can use form to manage multiple user interactions like - click ,pressing
 * enter etc.
 *
 * It also explains why we need to call event.preventDefault()
 *
 *  <form method="POST" action="/search">
 *  </form>
 *
 * If we omit the action attribute, the browser will use the current URL, effectively reloading
 *  the page.
 */
