/**
 * a)React has a built-in event delegation system that implements a bunch of optimizations for us
 * b)SyntheticBaseEvent object - Link o react Docs provided
 * c)how browser handles form submission, when some required field is not provided by user.
 *  can we get what was the error , if required and pattern attributes are provided?
 *
 * d)when we need to customize the dropdown or checkboxes for react, how do we handle accessibility?
 */
